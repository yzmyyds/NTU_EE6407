\documentclass[a4paper,12pt]{article}
\usepackage{mathptmx}
\usepackage{enumitem} 
\usepackage{geometry}
\usepackage{amsmath}
\usepackage{setspace}
\usepackage{graphicx}
\usepackage{caption}
\usepackage{float}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{hyperref}
\usepackage{listings}
\usepackage{color}
\usepackage[utf8]{inputenc}  
\usepackage{cite}  
\usepackage{booktabs}
\usepackage{indentfirst}
\geometry{margin=1in}

\begin{document}

\begin{titlepage}
    \centering
    \includegraphics[width=8cm]{Images/Nanyang_Technological_University-Logo.wine.png}\par\vspace{1cm}
    {\scshape\LARGE Nanyang Technological University \par}
    \vspace{1.5cm}
    {\huge\bfseries EE6407 Genetic Algorithms \& Machine Learning\par}
    \vspace{0.5cm}
    {\Large Assignment 1 \& 2 Report\par}
    \vspace{2cm}
    {\large MSc in Electrical and Electronic Engineering\par}
    {\large Yan Ziming\par}
    {\large G2507084J\par}
    \vfill
    {\large \today \par}
\end{titlepage}

\newpage
\tableofcontents
\newpage

\section{Introduction}
For the course EE6407, I implement two assignments, including the Naïve Bayes Classifier and the Fisher Linear Discriminate Classifier. This report summarizes the methods and results of these two assignments.
I use Python as the programming language and Jupyter Notebook as the developement environment. The followings show the details of my design and implementation.
\section{Assignment 1}
\subsection{Handling Missing Values and Outliers}

\subsubsection{Missing values}
\textbf{\textit{Detection:}}
  
For the missing value detection, I use the pandas library to check, with isna() function. This function would return a matrix reflects if the content in each position is empty or not. By summing the values in 
each column, I can determine the number of times each feature's missing values occur \ref{fig:code_cell_missing}.\\
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{Images/Assignment_1/missing_check.png}
    \caption{Missing Value Detection Code}
    \label{fig:code_cell_missing}
\end{figure}. 

At first, there seems no missing values. however, after trying to transform datat type from dtype to float, there seems several data cannot successfully transformed, leading to new missing values. The missing value detection for training set consequence \ref{tab:missing_value_count}:
\begin{table}[H]
    \centering
    \begin{tabular}{|c|c|}
        \hline
        Feature&Missing Value Count\\
        \hline
        A&2\\
        \hline
        B&0\\
        \hline
        C&0\\
        \hline
        D&1\\
        \hline
        E&0\\
        \hline
    \end{tabular}
    \caption{Missing Value Count}
    \label{tab:missing_value_count}
\end{table}
\textbf{\textit{Handling:}}
  
Since there are only several missing values in each feature, ig. two for feature A and one for feature D. I think that there are three methods could be used to replace the empty with practical values \cite{ren_review_2023}:
\begin{itemize}
    \item \textit{Missing Deletion:} 
    
    Simply delete all the feature at the position of missing value. This method is simple and effective, but may lead to data loss and bias of training classifier. In this condition, since the total number of missing values is 3, which is 2.5\% of the total data,
    I think this method may be acceptable.
    \item \textit{Single Imputation:} 
    
    Replace the missing value with a specific value, such as mean, median, or k nearest neighbors. This method avoid the data loss, and consider the distribution of data. However, when there are plenty of missing value, this method may replace these values with same data, leading to bias.
    \item \textit{Imputation with Machine Learning:} 
    
    Use machine learning methods, regression, to predict the missing value. For example, use linear regression, decision tree regression, or least square regression to predict. Compared with single imputation, this method is more reliable. But it is more complex and time-consuming.
\end{itemize}
  
In this assignment, I use the mean value of each feature to replace the missing values. Because the number of missing values is small, I think there is no need to use more complex methods, like machine learning. And this implementation is more practical than deletion \ref{fig:code_cell_fill}.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{Images/Assignment_1/imputation_missing.png}
    \caption{Imputation of Missing Values}
    \label{fig:code_cell_fill}
\end{figure}
\subsubsection{Outliers}
\textbf{\textit{Detection:}}
  
As for the outlier data, which means the data that is far away from the main distribution of dataset and may affect the performance of classifier. There are two mainstream methods to detect outliers, IQR and Z score. Because the dataset is small and may not follow Gaussian distribution. Therefore, I used 
the IQR method to detect, which calculates the IQR value with the first and third quartile ($Q_1$,$Q_3$), regarding values that out of the range [$Q_1-1.5\times IQR$ , $Q_3+1.5 \times IQR$] as outliers.
I used the following code \ref{fig:code_cell_IQR} to implement the number of outliers in each feature \ref{tab:outlier_count}. The training dataset has 5 outliers in total, while there is no outlier in test dataset.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{Images/Assignment_1/IQR.png}
    \caption{IQR Method Realization}
    \label{fig:code_cell_IQR}
\end{figure} 
\begin{table}[H]
    \centering
    \begin{tabular}{|c|c|}
        \hline
        Feature&Outlier Count\\
        \hline
        A&1\\
        \hline
        B&2\\
        \hline
        C&1\\
        \hline
        D&1\\
        \hline
        E&0\\
        \hline
    \end{tabular}
    \caption{Outlier Count}
    \label{tab:outlier_count}
\end{table}

In order to show the context of outliers, I visualized the distribution of each feature with boxplot \ref{fig:boxplot}.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{Images/Assignment_1/Boxplot_Train.png}
    \caption{Boxplot of Training Set}
    \label{fig:boxplot}
\end{figure}
\textbf{\textit{Handling:}}

Similar to the missing value, I come up with three methods may be used to handle the outliers.
\begin{itemize}
    \item \textit{Outlier Removeal:}

    This is the simplest and most direct method, just remove the outlier data with all features. This is effective, but may lead to data loss and training errors.
    \item \textit{Imputation:}

    Replace the outlier data with a specific value, such as mean and median. If there are many outliers, this method may lead to too many same values and affect the training of classifiers.
    \item \textit{Capping Method:}

    As mentioned before, the outliers are the values that out of IQR range. This method replace the too high value with the value of 95th percentile, and replace the too low value with the value of 
    5th percentile. This method can meep the distribution, but may have same demerits as imputation with many outliers.
\end{itemize}

In this assignment, I use the capping method to handle the outliers \ref{fig:code_cell_capping}, and visualize the dataset with boxplot after cleaning \ref{fig:boxplot_clean}.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{Images/Assignment_1/capping.png}
    \caption{Capping Realization}
    \label{fig:code_cell_capping}
\end{figure}
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{Images/Assignment_1/Boxplot_Train_Capped.png}
    \caption{Boxplot of Training Set After Cleaning}
    \label{fig:boxplot_clean}
\end{figure}

\subsection{Design a Naïve Bayes Classifier}
A Naïve Bayes Classifier is a simple classification model based on Bayes' Theorem with one important assumption: each feature is independent with each other (idd). I will introduce the establishment of classifier in the following parts.

What we want to compute for prediction is $P(C_k|\mathbf{x})$. According to Bayes' Theorem,
\begin{equation}
P(C_k \mid \mathbf{x}) = \frac{P(\mathbf{x} \mid C_k) \, P(C_k)}{P(\mathbf{x})}
\label{eq:bayes}
\end{equation}

The $\mathbf{x}$ has many features:[$x_1$, $x_2$, $x_3$, ..., $x_n$]. Since the assumption of idd, we can simplify the $P(\mathbf{x} \mid C_k)$:
\begin{equation}
P(\mathbf{x} \mid C_k) = P(x_1, x_2, x_3, ..., x_n \mid C_k) = \prod_{i=1}^{n} P(x_i \mid C_k)
\label{eq:idd}
\end{equation}

Therefore,
\begin{equation}
P(C_k \mid \mathbf{x})=\frac{P(C_k)\,\prod_{i=1}^{n} P(x_i \mid C_k)}{P(\mathbf{x})}
\end{equation}

According to maximum a posterior (MAP) estimation,
\begin{equation}
\hat{C} = \arg\max_{C_k}\frac{P(C_k)\,\prod_{i=1}^{n} P(x_i \mid C_k)}{P(\mathbf{x})}
\end{equation}

For all classes, $P(\mathbf{x})$ is constant. Therefore, we can ignore it and use log function to avoid underflow:
\begin{equation}
\hat{C} = \arg\max_{C_k}\left(\ln P(C_k) + \sum_{i=1}^{n} \ln P(x_i \mid C_k)\right)
\label{eq:discriminate}
\end{equation}

The (\ref{eq:discriminate}) shows the decision function (rule) of Naive Bayes Classifier. $P(C_k)$ could be estimate by the ratio of class $C_k$ in training set. 

As for the $P(x_i \mid C_k)$, since the data is continuous and not binary, we can assume that they follow Gaussian distribution. With the Maximum Likelihood Estimation
(MLE), we can estimate the mean and variance of each feature in each class. Therefore,
\begin{equation}
P(x_i \mid C_k) = \frac{1}{\sqrt{2\pi\sigma_{ik}^2}} \exp\left(-\frac{(x_i - \mu_{ik})^2}{2\sigma_{ik}^2}\right)
\end{equation}
\begin{equation}
    \mu_{ik} = \frac{1}{N_k} \sum_{x \in C_k} x_i
\end{equation}
\begin{equation}
    \sigma_{ik}^2 = \frac{1}{N_k} \sum_{x \in C_k} (x_i - \mu_{ik})^2
\end{equation}

With the above equations, I successfully establish a Gaussian Naive Bayes Classifier, the coding realization is shown \ref{fig:code_cell_1}.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{Images/Assignment_1/gnb_1.png}
    \label{fig:code_cell_0}
\end{figure}
\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{Images/Assignment_1/gnb_2.png}
    \caption{Code of Classifier Establishment}
    \label{fig:code_cell_1}
\end{figure}

\subsection{Prediction and Analysis}
After the development of classifier, I trained the model with training dataset, A-D for features and E for label. Then I predict the test dataset labels. In addition, I tested the accuracy with training dataset and it seems pretty good. The consequences are shown below.
\input{Images/Assignment_1/results_table.tex}

\section{Assignment 2}
\subsection{Fisher Linear Discriminate Classifier}
\subsubsection{Establishment of FLD}
The Fisher LInear Discriminate (FLD) is a classifier to classify datasets which only have two classes. The main idea of FLD is to project the data into a line and maximize the distance between two classes while minimize the variance in each class. The following parts introduce the establishment roughly. Namely,
we want to find a projection vector $\mathbf{w}$, which can maximize the following function:
\begin{equation}
    J(\mathbf{w}) = \frac{\mathbf{w}^T \mathbf{S_B} \mathbf{w}}{\mathbf{w}^T \mathbf{S_W} \mathbf{w}}
\end{equation}

(\textbf{i}) \textit{Get the label of two classes.}

For the unknown dataset, the first thing is to get the labels of two classes \ref{fig:code_cell_get_classes}. After coding, the labels of two classes are [-1, 1].
\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{Images/Assignment_2/label_get.png}
    \caption{Get Classes Code}
    \label{fig:code_cell_get_classes}
\end{figure}

(\textbf{ii}) \textit{Find the distance between two classes.}

The distance between two classes is the distance between the mean of each class. Therefore, I need to calculate the mean of each class first \ref{fig:code_cell_mean}, then calculate the distance \ref{fig:code_cell_distance}.
\begin{equation}
    \mathbf{m_k} = \frac{1}{N_k} \sum_{x \in C_k} \mathbf{x}
\end{equation}
\begin{equation}
    \mathbf{d} = \mathbf{m_1} - \mathbf{m_2}
\end{equation}

(\textbf{iii}) \textit{Calculate the within-class scatter matrix.}
\subsubsection{Training and Parameters}
After setting up the FLD classifier, I trained the model with training dataset and got the weights vector $\mathbf{w}$ and bias $b$ with the function inside class code. The parameters are shown below.
\input{Images/Assignment_2/parameter.tex}
\subsection{Prediction and Analysis}
\input{Images/Assignment_2/fisherlda_results.tex}
\section{Conclusion}

\newpage
\bibliographystyle{ieeetr}
\bibliography{references}
\end{document}